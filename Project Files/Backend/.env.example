# ────────────────────────────────────────────────
# LearnHub Backend Environment Variables
# ────────────────────────────────────────────────
# Copy this file to .env and fill in the values
# cp .env.example .env
#
# ⚠️  NEVER commit your .env file to version control!
# ────────────────────────────────────────────────

# Server Configuration
NODE_ENV=development
PORT=5000

# MongoDB Configuration
# Local:      mongodb://localhost:27017/learnhub
# Atlas:      mongodb+srv://<user>:<password>@cluster.xxxxx.mongodb.net/learnhub
MONGODB_URI=mongodb://localhost:27017/learnhub
MONGO_POOL_SIZE=10

# JWT Configuration (REQUIRED)
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS in production)
# Comma-separated for multiple origins: https://app.com,https://www.app.com
FRONTEND_URL=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File Upload
MAX_FILE_SIZE=104857600
