# LearnHub Frontend Environment Variables
# Copy to .env.development (local) or .env.production (deployed)
#
# For LOCAL development: leave empty (uses Vite proxy)
# For PRODUCTION: set to your deployed backend URL

# API URL (no trailing slash)
VITE_API_URL=
